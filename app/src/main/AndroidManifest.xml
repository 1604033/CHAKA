<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sibbir.cyclesharingapps">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".welcome_page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".blogin_page" />
        <activity android:name=".rlogin_page" />
        <activity android:name=".bregistration" />
        <activity android:name=".rregistration" />
        <activity android:name=".biker_choice" />
        <activity android:name=".rider_choice" />
        <activity android:name=".cycle_list" />
        <activity android:name=".provide" />
        <activity android:name=".remove" />
        <activity android:name=".shortestpath" />
        <activity android:name=".showpath"></activity>
    </application>

</manifest>